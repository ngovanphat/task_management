version: '3.8'

services:
  db:
    image: postgres
    restart: always
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: phatnv
      POSTGRES_PASSWORD: s@S11111
      POSTGRES_DB: task_management
